<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- All permissions, the Application class, MainActivity (launcher), the
         FileProvider and the FCM service are declared in :core and merged in. -->
</manifest>
